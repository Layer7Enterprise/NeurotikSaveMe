#include <CCup.h>

//Test the things sent to the web
void TestNetworkDebug() {
  It("Has the right neurons set for debug", function() {
  });

  It("Sends out the voltages for the right neurons", function() {
  });
}
